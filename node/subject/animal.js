const { v4 : uuidv4 } = require('uuid')
const { orgCol, userCol } = require('../resources')

const createNewSubject = (sName, parentId) => {
    const subject = {
        id: uuidv4(),
        
    }
}